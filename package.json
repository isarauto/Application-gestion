{
  "name": "gestion-location",
  "version": "3.0.0",
  "description": "Application de gestion comptable pour location de voitures (offline, SQLite)",
  "private": true,
  "scripts": {
    "serve": "npx http-server www -p 8080",
    "sync": "npx cap sync android",
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/share": "^7.0.0",
    "@capacitor-community/sqlite": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
